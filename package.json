{
  "name": "parcel-jquery",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "parcel",
    "build": "rm -rf ./dist/* && parcel build index.html",
    "deploy:gh-pages":
      "rm -rf node_modules/.cache && yarn build && git add -f dists && git commit -m \"Deploy parcel to gh-pages\" && git subtree push --prefix dist origin gh-pages"
  },
  "devDependencies": {
    "parcel-bundler": "^1.7.0"
  },
  "dependencies": {
    "jquery": "^3.3.1"
  }
}
